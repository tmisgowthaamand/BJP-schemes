# Frontend (Vite) LOCAL dev config.
# Points at the local backend on port 5000. The API client appends "/api"
# automatically (see src/utils/api.js). Vite dev server runs on port 3000.
VITE_API_URL=http://localhost:5000
