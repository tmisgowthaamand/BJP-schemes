# Frontend (Vercel) build-time config.
# Point this at your deployed Render backend base URL (NO trailing slash, NO /api).
# The API client appends "/api" automatically (see src/utils/api.js).
# Replace with your actual Render service URL if different.
VITE_API_URL=https://bjp-scheme.onrender.com
