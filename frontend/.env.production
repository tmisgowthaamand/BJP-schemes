# Frontend (Vercel) build-time config.
# This app is hosted at: https://bjp-schemes.vercel.app
# VITE_API_URL must point at the deployed Render BACKEND base URL
# (NO trailing slash, NO /api). The API client appends "/api" automatically
# (see src/utils/api.js). Replace with your actual Render service URL if different.
VITE_API_URL=https://bjp-schemes.onrender.com
